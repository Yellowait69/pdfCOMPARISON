<Window x:Class="PDFComparison.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PDFComparison.ViewModels"
        Title="PDF Comparator Pro - Enterprise Edition" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        FontFamily="Segoe UI Variable, Segoe UI">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <SolidColorBrush x:Key="PrimaryBlue" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#059669"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSubtle" Color="#64748B"/>

        <Style TargetType="Button" x:Key="ModernActionButton">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CBD5E1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="PDF Comparator Pro" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                <TextBlock Text="Compare documents folders and generate visual audits" FontSize="14" Foreground="{StaticResource TextSubtle}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content=" Start Comparison" Command="{Binding StartComparisonCommand}"
                        Style="{StaticResource ModernActionButton}" FontSize="16" FontWeight="Bold"
                        FontFamily="Segoe MDL2 Assets"
                        IsEnabled="{Binding IsNotProcessing}"/>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,25">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Opacity="0.08" BlurRadius="30" ShadowDepth="4"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" Foreground="{StaticResource PrimaryBlue}"/>
                    <TextBlock Text="Source Folder" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Text="{Binding SourceDirectory}" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="10,0"/>
                <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseSourceCommand}"
                        Style="{StaticResource ModernActionButton}" Background="#F1F5F9" Foreground="{StaticResource TextDark}" Padding="15,8"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" Foreground="{StaticResource SuccessGreen}"/>
                    <TextBlock Text="Target Folder" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                </StackPanel>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TargetDirectory}" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="10,0"/>
                <Button Grid.Row="2" Grid.Column="2" Content="Browse" Command="{Binding BrowseTargetCommand}"
                        Style="{StaticResource ModernActionButton}" Background="#F1F5F9" Foreground="{StaticResource TextDark}" Padding="15,8"/>

                <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" Foreground="#F59E0B"/>
                    <TextBlock Text="Output Path" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                </StackPanel>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding OutputDirectory}" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="10,0"/>
                <StackPanel Grid.Row="4" Grid.Column="2" Orientation="Horizontal">
                    <Button Content=" Open" Command="{Binding OpenOutputDirectoryCommand}"
                            Style="{StaticResource ModernActionButton}" Background="#F1F5F9" Foreground="{StaticResource TextDark}"
                            FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" Padding="15,8"/>
                    <Button Content=" Share" Command="{Binding ShareViaOutlookCommand}"
                            Style="{StaticResource ModernActionButton}" Background="{StaticResource SuccessGreen}"
                            FontFamily="Segoe MDL2 Assets" Padding="15,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="5,0,5,25">
            <Grid Margin="0,0,0,10">
                <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="{StaticResource TextDark}" FontWeight="Medium"/>
                <TextBlock HorizontalAlignment="Right" Foreground="{StaticResource PrimaryBlue}" FontWeight="Bold">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}Process: {0} / {1}">
                            <Binding Path="ProgressValue" />
                            <Binding Path="ProgressMax" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
            <ProgressBar Minimum="0" Maximum="{Binding ProgressMax}" Value="{Binding ProgressValue}"
                         Height="10" Foreground="{StaticResource PrimaryBlue}" Background="#E2E8F0" BorderThickness="0">
                <ProgressBar.Template>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                            <Border x:Name="Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="5"/>
                        </Grid>
                    </ControlTemplate>
                </ProgressBar.Template>
            </ProgressBar>
        </StackPanel>

        <Border Grid.Row="3" Background="White" CornerRadius="16" Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Opacity="0.05" BlurRadius="20" ShadowDepth="2"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Pairs}" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column"
                      GridLinesVisibility="None" BorderThickness="0" Background="Transparent"
                      RowHeight="50" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSubtle}"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F1F5F9"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding MatchKey}" Width="60"/>

                    <DataGridTemplateColumn Header="Source Document" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="#EF4444" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding SourcePath}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Time" Binding="{Binding CompletedTime, StringFormat=\{0:HH:mm\}}" Width="70" Foreground="{StaticResource TextSubtle}"/>

                    <DataGridTemplateColumn Header="Status" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" CornerRadius="12" HorizontalAlignment="Left" x:Name="statusBorder">
                                    <TextBlock Text="{Binding Status}" FontWeight="Bold" FontSize="11"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Different">
                                        <Setter TargetName="statusBorder" Property="Background" Value="#FEE2E2"/>
                                        <Setter TargetName="statusBorder" Property="TextBlock.Foreground" Value="#EF4444"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Identical">
                                        <Setter TargetName="statusBorder" Property="Background" Value="#D1FAE5"/>
                                        <Setter TargetName="statusBorder" Property="TextBlock.Foreground" Value="#059669"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Diff Details" Binding="{Binding ErrorMessage}" Width="150" FontStyle="Italic" Foreground="{StaticResource TextSubtle}"/>

                    <DataGridTemplateColumn Header="Action" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View Report"
                                        Command="{Binding DataContext.OpenReportCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding HasReport, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Style="{StaticResource ModernActionButton}" Padding="12,5" FontSize="12" Background="{StaticResource PrimaryBlue}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>