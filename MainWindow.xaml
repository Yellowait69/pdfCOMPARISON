<Window x:Class="PdfComparer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PdfComparer.ViewModels"
        Title="Comparateur PDF Automatisé" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>ggg
        </Grid.RowDefinitions>

        <GroupBox Header="Configuration des dossiers" Grid.Row="0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto" Margin="0,10,0,0"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Dossier Source :" VerticalAlignment="Center" Width="100"/>
                <TextBox Grid.Column="1" Text="{Binding SourceDirectory, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

                <TextBlock Grid.Row="1" Text="Dossier Target :" VerticalAlignment="Center" Width="100"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TargetDirectory, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
            </Grid>
        </GroupBox>

        <Button Grid.Row="1" Content="Lancer la Comparaison" Command="{Binding StartComparisonCommand}"
                Margin="0,15,0,15" Padding="10" FontWeight="Bold" Background="#0078D7" Foreground="White"
                IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBoolConverter}}"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ProgressBar Minimum="0" Maximum="{Binding ProgressMax}" Value="{Binding ProgressValue}" Height="20"/>
        </StackPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Pairs}" AutoGenerateColumns="False"
                  IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F0F0F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Clé" Binding="{Binding MatchKey}" Width="80"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Fichier Source" Binding="{Binding SourcePath}" Width="*"/>
                <DataGridTextColumn Header="Erreurs" Binding="{Binding ErrorMessage}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>